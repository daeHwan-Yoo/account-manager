#ifndef __BANKING_COMMON_H__
#define __BANKING_COMMON_H__

#include <iostream>
#include <cstring>

using namespace std;
const int NAME_LEN = 30;

enum {MAKE=1, DEPOSIT, WITHDRAW, INQUIRE, EXIT};

enum { LEVEL_A = 10, LEVEL_B = 5, LEVEL_C = 3 };

enum { NORMAL = 1, CREDIT = 2 };

#endif
